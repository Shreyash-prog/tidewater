"""Tidewater CDK application package.

`infra` is a real package (not a sys.path root) so the local `infra.constructs`
subpackage never shadows the CDK `constructs` library. The app is invoked as
`python -m infra.app` with the repo root as the working directory; see the
root-level cdk.json.
"""
