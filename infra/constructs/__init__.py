"""Reusable CDK constructs for Tidewater.

Imported as `infra.constructs.*` (a subpackage of `infra`), which is why it can
safely share the name `constructs` with the CDK library — the two never collide
on sys.path. See docs/architecture.md §2.
"""
