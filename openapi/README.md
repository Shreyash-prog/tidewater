# OpenAPI

`api.yaml` will be the hand-edited source of truth for the dashboard API
contract. The TypeScript client (`dashboard/src/api/generated/`) is generated
from it via `openapi-typescript` + `openapi-fetch`.

The spec is written in Phase 8 (docs/build-plan.md), not yet present.
